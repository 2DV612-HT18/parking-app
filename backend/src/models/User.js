export default class User {
  constructor(role, firstName, lastName, email, personalNumber, password) {
    this.role = role;
    this.firstName = firstName;
    this.lastName = lastName;
    this.email = email;
    this.personalNumber = personalNumber;
    this.password = password;
  }
}